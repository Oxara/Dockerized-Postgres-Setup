# Environment Configuration Template
# Copy this file to create environment-specific .env files
# NEVER commit real .env files to Git!

# ============================================
# KLASÖR YAPISI
# ============================================
# Her ortam kendi klasöründe çalışır:
#
# environments/
# ├── dev/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# ├── test/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# └── prod/
#     ├── docker-compose.yml
#     └── .env              <- Bu dosyayı buraya kopyalayın
#
# ============================================
# KULLANIM
# ============================================
# 1. Bu dosyayı ilgili ortam klasörüne kopyalayın:
#    cp .env.example dev/.env
#    cp .env.example test/.env
#    cp .env.example prod/.env
#
# 2. Her ortam için portları ayarlayın:
#    - dev:  MONGO_PORT=27017, MONGOEXPRESS_PORT=8081
#    - test: MONGO_PORT=27018, MONGOEXPRESS_PORT=8082
#    - prod: MONGO_PORT=27019, MONGOEXPRESS_PORT=8083
#
# 3. Güçlü şifreler belirleyin (özellikle prod için!)
#
# 4. Ortamı başlatın:
#    cd dev && docker-compose up -d
#    veya
#    .\manage.ps1 start dev mongodb
#
# ============================================
# YAPILANDIRMA
# ============================================

# MongoDB Settings
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=CHANGE_THIS_PASSWORD
MONGO_PORT=27017  # dev: 27017, test: 27018, prod: 27019

# Mongo Express Settings
MONGOEXPRESS_LOGIN=admin
MONGOEXPRESS_PASSWORD=CHANGE_THIS_PASSWORD
MONGOEXPRESS_PORT=8081  # dev: 8081, test: 8082, prod: 8083

# ============================================
# GÜVENLİK UYARILARI
# ============================================
# ⚠️  ASLA varsayılan şifreleri production'da kullanmayın!
# ⚠️  Her ortam için FARKLI şifreler kullanın!
# ⚠️  Şifreleri Git'e YÜKLEMEYIN!
# ⚠️  Güçlü şifreler: En az 16 karakter, büyük/küçük harf, rakam, özel karakter
#
# Şifre Örneği: Mg9&nD2$bL8@xR5#wP3!
#
# ============================================
# PORT DAĞILIMI
# ============================================
# Ortam      | MongoDB | Mongo Express
# -----------|---------|---------------
# Development| 27017   | 8081
# Test       | 27018   | 8082
# Production | 27019   | 8083
#
# ============================================
# NOTLAR
# ============================================
# - MongoDB Connection: mongodb://admin:password@localhost:27017
# - Mongo Express UI: http://localhost:8081
# - Default Database: admin
# - Data persistence: Volume'lerde saklanır
# - Container restart policy: unless-stopped (dev/test), always (prod)
# - Her ortam kendi network'ünde izole çalışır
