# n8n — Production Environment
# Copy this file to .env before starting the service:
#   cp .env.example .env
#
# ⚠️  SECURITY: Replace ALL placeholder values with strong, unique secrets!

# n8n Web UI host port
N8N_PORT=5680

# n8n host / protocol — set to your real domain in production
N8N_HOST=yourdomain.com
N8N_PROTOCOL=https

# Webhook base URL — must be publicly reachable
WEBHOOK_URL=https://yourdomain.com:5680/

# Basic auth credentials for the web UI
# Use a strong, unique password (min 20 chars, mixed case + digits + special)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Encryption key for storing credentials securely — NEVER change after first run!
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32

# Timezone
GENERIC_TIMEZONE=Europe/Istanbul
