# Environment Configuration Template
# Copy this file to create environment-specific .env files
# NEVER commit real .env files to Git!

# ============================================
# KLASÖR YAPISI
# ============================================
# Her ortam kendi klasöründe çalışır:
#
# environments/
# ├── dev/
# │   ├── docker-compose.yml
# │   ├── prometheus.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# ├── test/
# │   ├── docker-compose.yml
# │   ├── prometheus.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# └── prod/
#     ├── docker-compose.yml
#     ├── prometheus.yml
#     └── .env              <- Bu dosyayı buraya kopyalayın
#
# ============================================
# KULLANIM
# ============================================
# 1. Bu dosyayı ilgili ortam klasörüne kopyalayın:
#    cp .env.example dev/.env
#    cp .env.example test/.env
#    cp .env.example prod/.env
#
# 2. Her ortam için portları ayarlayın:
#    - dev:  PROMETHEUS_PORT=9090, GRAFANA_PORT=3000
#    - test: PROMETHEUS_PORT=9091, GRAFANA_PORT=3001
#    - prod: PROMETHEUS_PORT=9092, GRAFANA_PORT=3002
#
# 3. Güçlü şifreler belirleyin (özellikle prod için!)
#
# 4. Ortamı başlatın:
#    cd dev && docker-compose up -d
#    veya
#    .\manage.ps1 start dev monitoring
#
# ============================================
# YAPILANDIRMA
# ============================================

# Prometheus Settings
PROMETHEUS_PORT=9090  # dev: 9090, test: 9091, prod: 9092

# Grafana Settings
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
GRAFANA_PORT=3000  # dev: 3000, test: 3001, prod: 3002

# ============================================
# GÜVENLİK UYARILARI
# ============================================
# ⚠️  ASLA varsayılan şifreleri production'da kullanmayın!
# ⚠️  Her ortam için FARKLI şifreler kullanın!
# ⚠️  Şifreleri Git'e YÜKLEMEYIN!
# ⚠️  Güçlü şifreler: En az 16 karakter, büyük/küçük harf, rakam, özel karakter
#
# Şifre Örneği: Pr9&mT2$hG8@fR5#wL3!
#
# ============================================
# PORT DAĞILIMI
# ============================================
# Ortam      | Prometheus | Grafana
# -----------|------------|--------
# Development| 9090       | 3000
# Test       | 9091       | 3001
# Production | 9092       | 3002
#
# ============================================
# NOTLAR
# ============================================
# - Prometheus UI: http://localhost:9090
# - Grafana UI: http://localhost:3000
# - Grafana varsayılan kullanıcı: admin
# - Data persistence: Volume'lerde saklanır
# - Container restart policy: unless-stopped (dev/test), always (prod)
# - Her ortam kendi network'ünde izole çalışır
# - Prometheus, Grafana'ya veri kaynağı olarak otomatik eklenir
