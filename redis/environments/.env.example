# Environment Configuration Template
# Copy this file to create environment-specific .env files
# NEVER commit real .env files to Git!

# ============================================
# KLASÖR YAPISI
# ============================================
# Her ortam kendi klasöründe çalışır:
#
# environments/
# ├── dev/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# ├── test/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# └── prod/
#     ├── docker-compose.yml
#     └── .env              <- Bu dosyayı buraya kopyalayın
#
# ============================================
# KULLANIM
# ============================================
# 1. Bu dosyayı ilgili ortam klasörüne kopyalayın:
#    cp .env.example dev/.env
#    cp .env.example test/.env
#    cp .env.example prod/.env
#
# 2. Her ortam için portları ayarlayın:
#    - dev:  REDIS_PORT=6379, REDISINSIGHT_PORT=8001
#    - test: REDIS_PORT=6380, REDISINSIGHT_PORT=8002
#    - prod: REDIS_PORT=6381, REDISINSIGHT_PORT=8003
#
# 3. Güçlü şifreler belirleyin (özellikle prod için!)
#
# 4. Ortamı başlatın:
#    cd dev && docker-compose up -d
#    veya
#    .\manage.ps1 start dev redis
#
# ============================================
# YAPILANDIRMA
# ============================================

# Redis Settings
REDIS_PASSWORD=CHANGE_THIS_PASSWORD
REDIS_PORT=6379  # dev: 6379, test: 6380, prod: 6381

# RedisInsight Settings
REDISINSIGHT_PORT=8001  # dev: 8001, test: 8002, prod: 8003

# ============================================
# GÜVENLİK UYARILARI
# ============================================
# ⚠️  ASLA varsayılan şifreleri production'da kullanmayın!
# ⚠️  Her ortam için FARKLI şifreler kullanın!
# ⚠️  Şifreleri Git'e YÜKLEMEYIN!
# ⚠️  Güçlü şifreler: En az 16 karakter, büyük/küçük harf, rakam, özel karakter
#
# Şifre Örneği: Kx9&mP2$vL8@qR5#wN3!
#
# ============================================
# PORT DAĞILIMI
# ============================================
# Ortam      | Redis | RedisInsight
# -----------|-------|-------------
# Development| 6379  | 8001
# Test       | 6380  | 8002
# Production | 6381  | 8003
#
# ============================================
# NOTLAR
# ============================================
# - Redis AOF (Append Only File) persistence aktif
# - Veriler volume'lerde saklanır
# - Container restart policy: unless-stopped
# - Her ortam kendi network'ünde izole çalışır
