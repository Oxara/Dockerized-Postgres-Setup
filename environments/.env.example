# Environment Configuration Template
# Copy this file to create environment-specific .env files
# NEVER commit real .env files to Git!

# ============================================
# KLASÖR YAPISI
# ============================================
# Her ortam kendi klasöründe çalışır:
#
# environments/
# ├── dev/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# ├── test/
# │   ├── docker-compose.yml
# │   └── .env              <- Bu dosyayı buraya kopyalayın
# └── prod/
#     ├── docker-compose.yml
#     └── .env              <- Bu dosyayı buraya kopyalayın
#
# ============================================
# KULLANIM
# ============================================
# 1. Bu dosyayı ilgili ortam klasörüne kopyalayın:
#    cp .env.example dev/.env
#    cp .env.example test/.env
#    cp .env.example prod/.env
#
# 2. Her ortam için portları ayarlayın:
#    - dev:  POSTGRES_PORT=5432, PGADMIN_PORT=5050, GRAFANA_PORT=3000
#    - test: POSTGRES_PORT=5433, PGADMIN_PORT=5051, GRAFANA_PORT=3001
#    - prod: POSTGRES_PORT=5434, PGADMIN_PORT=5052, GRAFANA_PORT=3002
#
# 3. Güçlü şifreler belirleyin (özellikle prod için!)
#
# 4. Ortamı başlatın:
#    cd dev && docker-compose up -d
#    veya
#    .\manage.ps1 start dev
#
# ============================================
# YAPILANDIRMA
# ============================================

# PostgreSQL Settings
POSTGRES_USER=postgres_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=postgres_db
POSTGRES_PORT=5432  # dev: 5432, test: 5433, prod: 5434

# pgAdmin Settings
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=CHANGE_THIS_PASSWORD
PGADMIN_PORT=5050  # dev: 5050, test: 5051, prod: 5052

# Grafana Settings
GRAFANA_PORT=3000  # dev: 3000, test: 3001, prod: 3002
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
